## What to build
Update the Main Dashboard (`index.html`) to reflect the new section-level assessments. This includes updating meta-data calculation to count total tasks and tests across the course and displaying these counts on the course cards.

## Acceptance criteria
- [ ] `LayoutGenerator` correctly calculates total section-level tasks and tests for a `Course`
- [ ] Course cards on `index.html` display assessment counts (e.g., "3 Tasks • 2 Tests")
- [ ] Formatting is consistent with existing "Sections • Lessons" meta-data
- [ ] Integration tests verify the dashboard HTML contains the expected counts

## Blocked by
- 1_section_tasks.md
- 2_section_tests.md
